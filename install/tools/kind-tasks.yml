- name: Install kind
  get_url:
    url: https://github.com/kubernetes-sigs/kind/releases/download/v0.5.1/kind-linux-amd64
    dest: /usr/bin/kind

- name: Make kind executable 
  file: path=/usr/bin/kind mode=a+x